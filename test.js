// Fichier de test pour tera change
function add(a, b) {
    return a + b;
  }
  
  function multiply(a, b) {
    return a * b;
  }
  
  function divide(a, b) {
    return a / b;
  }
  
  console.log(divide(10, 0)); // Test division par zéro
  
  function minus(a, b) {
    return a - b;
  }
  
  console.log(add(2, 3));
  console.log(multiply(4, 5));
  
console.log(minus(5, 3));  